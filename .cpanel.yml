deployment:
  tasks:
    - export DEPLOYPATH=/home/rosasuy/reserva-server/
    - /bin/mkdir -p $DEPLOYPATH/tmp
    - cd $DEPLOYPATH && npm install --omit=dev --no-package-lock --no-audit --no-fund
    - /bin/touch $DEPLOYPATH/tmp/restart.txt
